// This is the main function.
fn main() {
    // Statements here are executed when the compiled binary is called.

    // Notice here ! mark is used after println
    // In rust, ! is used with MACRO, ie. println is not a function
    // MACRO run faster than fucntions, as compiler contoll is not passed anywhere.
    println!("Hello World!");
}
